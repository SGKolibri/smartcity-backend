import { Module } from '@nestjs/common';

/**
 * Domínio: Postes.
 * Endpoints REST entram na Fase 3 do roadmap
 * (GET /postes, GET /postes/:id, PATCH /postes/:id/status).
 */
@Module({})
export class PostesModule {}
